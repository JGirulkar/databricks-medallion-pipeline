# CE assessment workspace — never commit tokens
DATABRICKS_CONFIG_PROFILE=de-assessment-ce
DATABRICKS_HOST=https://dbc-06f970f4-0f19.cloud.databricks.com

# GitHub MCP (project `.cursor/mcp.json`, JGirulkar only).
# Prefer: source scripts/env.sh (auto-fills from `gh auth token -u JGirulkar`).
# Or set manually after `gh auth login` as JGirulkar:
# GITHUB_DE_ASSESSMENT_TOKEN=
