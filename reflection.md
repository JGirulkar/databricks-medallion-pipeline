# Reflection

_(To be completed in my own words before submission. The bullet stubs under
each heading are pointers to material elsewhere in the repo — replace them
with prose, keep what is true, delete what is not.)_

## What I Built

<!-- the pipeline in 4-5 sentences; what is done (bronze+silver, verified
end to end on CE, two-delivery CDC) and what is next (gold, dashboard) -->

## How I Used AI (Across the Lifecycle)

<!-- your process: spec-first design dialogues, red→green fixes, background
E2E runs while working on other tasks, docs-truth passes.
Material: tool-workflow.md "working rhythm"; ai-prompts/05 P1-P10 -->

## What AI Helped With Most

<!-- e.g. speed of the mechanical layers; turning each of your catches into a
permanent test/guard; the 1-minute contract tier -->

## What AI Got Wrong

<!-- be specific, it is the strongest section. Material: debugging-notes.md —
missing imports that reached the cluster; the merge-vs-reset task duplication;
one-directional orphan healing (twice); non-deterministic survivorship;
justifying decisions by audience instead of merit until corrected -->

## How I Validated AI Output

<!-- never on its word: 139 tests, forbid-skips, contract tier with
independently derived expectations, table-level SQL after every run,
"which side is wrong" question when a test fails -->

## What I Would Improve Next

<!-- e.g. contract tier from day one instead of week two; orphan-flag design
before building parent-refresh; fewer cluster iterations -->

## Reusable Workflow

<!-- what you would carry to any pipeline: capture-raw/curate-deliberately
history, three-outcome quality model, state-derived flags over event-driven,
guards over diligence -->
