# Dashboard Setup (CE)

Create SQL dashboard with 3+ tiles per `cursor-workflow/spec.md`.

Document CE UI path used (Lakeview vs legacy).

See `databricks/jobs/gold/` SQL files when implemented.
